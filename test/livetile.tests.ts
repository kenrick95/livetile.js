import LiveTile from '../src/LiveTile'

describe('LiveTile test', () => {
  it('works if true is truthy', () => {
    expect(true).toBeTruthy()
  })

  // it('LiveTile is instantiable', () => {
  //   expect(new LiveTile()).toBeInstanceOf(LiveTile);
  // });
})
